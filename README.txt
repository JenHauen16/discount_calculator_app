This is a simple web app made with the Python library Streamlit.

This app calculates the discount to an item and its corresponding tax (6%) if a specific discount
is needed.

In its current form, it is run in the background on a Linux-based virtual machine on a Google Compute Engine.

To run the app, use the command: nohup streamlit run name_of_app.py &


